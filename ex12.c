#include<stdio.h>
//입력된 두 수의 큰 값을 찾아서 반환하는 함수 작성
//함수 이름 : bigNum
//입력 변수 : a,b
//결과 변수 : res

void main(){

	int a,b,res;
	scanf("%d",&a);
	scanf("%d",&b);
	res = bigNum(a,b);
	printf("big number : %d\n", res);
}
int bigNum(int numq, int num2)

	for(int a>b)
		printf(a);
		for(int a<b)
			printf(b);
				for(int a=b)
					printf("a와 b의 값이 같음");
					
}